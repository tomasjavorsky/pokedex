import { makeStyles, Theme } from '@material-ui/core'

export const styles = makeStyles((theme: Theme) => ({
    galerryContainer: {
        display: 'flex',
        flexWrap: 'wrap',
        width: '1056px'
    },
}))
